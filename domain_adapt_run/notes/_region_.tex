\message{ !name(formulation.tex)}\documentclass[8pt]{article}
\usepackage[letterpaper, margin=1in]{geometry}
\hyphenation{op-tical net-works semi-conduc-tor}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{mathtools}
\usepackage[numbers]{natbib}
\usepackage{subcaption}
\usepackage{bbm}
\newcommand{\tab}{\hspace*{2em}}
\newcommand{\ignore}[1]{}

\begin{document}

\message{ !name(formulation.tex) !offset(60) }
  Assuming the basis functions to be
non-negative, the optimization problem is then to learn the weights
parameterizing the ratio function:

\message{ !name(formulation.tex) !offset(360) }

\end{document}